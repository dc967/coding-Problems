#include<bits/stdc++.h>
using namespace std;
int main()
{

    vector<int> nums = {1,1,2,3,4,4,5};
    int n = nums.size();
    int left = 0;
    int right = left + 1;

    while(left < right){
       if()
    }

   return 0;
}