#include<bits/stdc++.h>
using namespace std;
int main()
{

   /* set<int> s ;
   s.insert(10);
   s.insert(20);
   s.insert(30);
   s.insert(10);

    for(int i=0;i<s.size();i++){
        if(s.find(i) != s.end()){
            cout << "True" << endl;
        }
        cout << "false" << endl;
    }*/

    char ch[]="apple";
    cout<<strlen(ch);


    
  

   return 0;
}